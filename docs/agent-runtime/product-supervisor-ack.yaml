---
schema_version: 1
channel: product_supervisor_ack
project: job-hunter
last_seen_directive_id: JH-SUP-0020
last_accepted_directive_id: JH-SUP-0020
last_applied_directive_id: JH-SUP-0020
status: SERPAPI_LIVE_PROOF_BLOCKED_CREDENTIAL
SPRINT_1_STATUS: NOT_DONE
blocker_class: BLOCKED_HUMAN_PERMISSION
updated_at: "2026-09-03T22:10:00Z"
updated_by: claude_active_orchestrator
head_sha: TBD
evidence:
  - docs/forensics/CLAUDE_GOOGLE_CHALLENGE_CAUSE_ANALYSIS.md
  - docs/forensics/CODEX_GOOGLE_CHALLENGE_CAUSE_ANALYSIS.md
  - docs/forensics/GOOGLE_CHALLENGE_CAUSE_RECONCILIATION.md
  - docs/design/CLAUDE_GOOGLE_SEARCH_BEST_PRACTICE.md
  - docs/design/CODEX_GOOGLE_SEARCH_BEST_PRACTICE.md
  - docs/design/GEMINI_GOOGLE_SEARCH_BEST_PRACTICE.md
  - docs/design/GOOGLE_SEARCH_BEST_PRACTICE_DECISION.md
  - docs/design/CHEAPEST_GOOGLE_SEARCH_COST_DECISION.md
  - docs/design/CLAUDE_CHROME_CONTAINER_FEASIBILITY.md
  - docs/evidence/SERPAPI_LIVE_PROOF_CEGINFO_VMK.md
  - docs/design/CHEAPEST_GOOGLE_SEARCH_COST_DECISION.md
notes: >
  JH-SUP-0013 executed exactly as specified. Before running, independently
  verified (not assumed) that this session is genuinely on the on-prem
  host: public IP 78.131.58.101, reverse DNS secure.vmk.hu (Product
  Owner's own organization), AS20845 DIGI Tavkozlesi es Szolgaltato Kft.
  (a real Hungarian ISP, not a cloud/datacenter provider), Szekesfehervar
  HU. Docker confirmed usable.

  Ran exactly: bash apps/google-browser-search/run-gate0-onprem.sh
  "IT vezeto". Result: GOOGLE_CHALLENGE / FAIL. The challenge screenshot
  explicitly names the real on-prem IP (78.131.58.101) as the blocked
  address, confirming this is not a proxy artifact -- the genuine
  organizational IP itself was challenged on the first request, headed
  Chromium, Docker/Xvfb, zero retries, zero bypass attempted.

  This is a significant finding: it weakens the datacenter-IP-reputation
  hypothesis that was the shared primary recommendation of JH-SUP-0011
  and JH-SUP-0012 (both mine and Codex's independent analyses). A
  genuine, verified, non-datacenter, organization-owned IP was blocked
  exactly like the cloud host was. Recorded honestly as changing the
  working hypothesis toward automation/browser-fingerprint-level signals
  being more likely than network origin, not as a repeat of the same
  already-known failure.

  No retry, no stealth/proxy/fingerprint bypass. Evidence committed and
  pushed. Stopping per directive scope -- awaiting the next Product Owner
  directive.

  JH-SUP-0013 hardened redelivery (2026-09-03T22:07, one-shot corporate
  egress safety limit) ACKed retroactively: re-inspected gate0-onprem.mjs
  and confirmed exactly one page.goto(googleUrl) call, no retry loop, no
  fallback search path -- the run already executed above already
  satisfied the hardened one-shot limit. Evidence file updated with an
  explicit "total Google search attempts sent: 1" statement. The
  one-shot budget for JH-SUP-0013 is fully consumed. No second Google
  request has been made or will be made under this directive. Real
  user separately raised the same corporate-network-reputation concern
  directly in chat; confirmed to them no further automated Google
  request will run without explicit new authorization.

  JH-SUP-0014 applied: hard stop on all further automated Google traffic
  from 78.131.58.101 (secure.vmk.hu / AS20845 DIGI) until explicit new
  Product Owner authorization. No scheduled/cron/systemd Job Hunter
  Google test existed to disable (checked: crontab -l and systemctl
  list-timers, none found referencing gate0). run-gate0-onprem.sh has
  not been and will not be run again absent new authorization.
  JH-SUP-0013 evidence preserved unchanged. Broad development freeze
  remains in force.

  JH-SUP-0015 applied: static forensic analysis of the JH-SUP-0013 Google
  challenge, zero new Google traffic (per directive safety invariant).
  Claude and Codex each wrote independent analyses from the same evidence
  (Codex additionally read local Playwright 1.62.1 source directly for
  exact default launch-flag behavior). Reconciliation: team conclusion is
  that automation/session-fingerprint signals and IP/network reputation-
  or-history are both live, roughly comparable-plausibility contributors,
  more likely combined than either alone sufficient. Datacenter-IP-
  reputation as sole/primary cause is weakened, not eliminated. Exact
  Google decision weighting cannot be determined without Google's
  internal telemetry -- stated explicitly as unknowable. No retry, no
  bypass, no new Google request. JH-SUP-0014 hard stop remains in force.

  JH-SUP-0016 applied: Claude, Codex, and Gemini each researched
  independently (Gemini mandatory per this directive, not optional).
  Zero new Google Search traffic. Team consensus: literal browser
  automation of google.com/search is a documented Google ToS violation,
  not just a detection problem -- reject as production architecture.
  The real official Google Web Search Service API (WSS,
  websearchservice.googleapis.com) is the preferred target, gated
  behind a Google partner agreement + client_id, no self-service path
  found. Custom Search JSON API rejected as a new foundation (closed to
  new customers, EOL 2027-01-01). Self-correction recorded: an initial
  Claude check of a bare/incomplete URL wrongly concluded WSS did not
  exist; corrected after cross-checking Codex's finding against the
  live docs. Gemini's report had one factual error (wrong WSS
  endpoint/hostname) caught and corrected in the reconciliation; its
  overall recommendation was otherwise independently corroborated by
  Claude and Codex. Gemini dispatch note: the shared gemini-freshcheck
  channel was mid-task on an unrelated project when queried; request
  completed after that work rather than interrupting it -- disclosed
  in the decision doc. Recommendation to Product Owner: amend
  SPRINT_1.md from literal browser-SERP wording to an outcome
  requirement, and pursue WSS partner contact (no Google traffic
  required) as the smallest safe next action. Awaiting Product Owner
  decision -- no implementation started under this directive.

  JH-SUP-0016 redelivered (cheapest-cost + Gemini mandatory + account/
  incognito questions) and JH-SUP-0017 (near-duplicate cost question,
  explicit Gemini prompt required) both applied together -- overlap
  disclosed in the decision doc. Claude, Codex, Gemini each answered
  independently (Codex fact-check role per JH-SUP-0017's framing).
  Zero new Google traffic. Updated cheapest-first finding: at the real
  ~8-10 searches/month volume, SerpApi's free tier (250/month, $0,
  returns ranked organic title+URL) is now the recommended immediate
  path, not WSS -- WSS pricing remains unconfirmed/partner-gated and
  is no longer the near-term recommendation, though still worth
  pursuing in parallel long-term. Gemini Grounding is the cheapest
  official first-party Google option ($0 at this volume) but returns
  synthesized-answer+citations, not a raw ranked SERP list -- flagged
  as a different data shape, not a drop-in equivalent. One correction
  applied to Gemini's report: it ranked Custom Search JSON API as a
  viable $0 option without noting it is closed to new customers since
  2025 and EOL 2027-01-01 -- fixed in the reconciliation. No Google
  account required or recommended; Incognito-vs-Playwright difference
  explained as a stack of automation/session signals, not proven to a
  single cause. No implementation started -- awaiting Product Owner
  decision on SPRINT_1.md amendment and vendor selection.

  JH-SUP-0019 applied: feasibility review of the JH-SUP-0018 Claude Code
  CLI + Claude in Chrome unattended worker, per Product Owner's
  sequencing correction, BEFORE any build/run. Zero Google traffic --
  paused JH-SUP-0018 build per this directive's own execution
  constraint (docker build/run was not performed). Verdict:
  NOT_VIABLE_AS_DESIGNED for the stated unattended twice-weekly goal.
  Verified directly against official Anthropic docs
  (code.claude.com/docs/en/chrome, fetched live): Chrome integration
  requires interactive /login on a direct Anthropic Pro/Max/Team/
  Enterprise plan -- API-key/long-lived-token auth (what an unattended
  job would use) explicitly disables it. State-changing browser
  actions require human approval by default. No Docker/headless/
  server deployment is documented anywhere; the product assumes a
  human watching a visible Chrome window. This is a hard architectural
  blocker, not an engineering gap to route around. Recommendation:
  do not continue the unattended-worker build; adopt the already-
  reconciled SerpApi free-tier plan (JH-SUP-0016/0017) for the actual
  Sprint 1 search need. If Product Owner wants a human-supervised
  Chrome+Claude assistant tool for other purposes, scope that
  separately -- it is a different, smaller task, not Sprint 1's
  automation mechanism. Full evidence and reasoning in
  docs/design/CLAUDE_CHROME_CONTAINER_FEASIBILITY.md. Stopped per
  directive -- awaiting Product Owner decision before any further
  build action.

  JH-SUP-0020 applied: attempted live SerpApi proof search for
  "CEGINFO WWW.VMK.HU". No Google traffic; SerpApi is the required
  path per the reconciled JH-SUP-0016/0017 recommendation. Checked
  environment variables, repo/.env files, filesystem for anything
  named *serpapi*, and attempted gh secret/variable list (gh CLI not
  authenticated in this session) -- no usable SerpApi API key found
  anywhere accessible. No already-authorized SerpApi project/service
  identity exists. Per directive, did not fabricate credentials or
  attempt account signup under an invented identity -- SerpApi
  signup requires real email + verification, an unavoidable human
  step. Result: BLOCKED_CREDENTIAL, recorded honestly with the exact
  blocker in docs/evidence/SERPAPI_LIVE_PROOF_CEGINFO_VMK.md. No JSON
  evidence file created (nothing genuine to save -- no API call was
  made). Ready to execute the exact same query in one follow-up run
  as soon as a real SerpApi key is supplied via an approved secret
  channel. Stopped per directive's own "unavoidable credential
  blocker" stop condition.
