---
schema_version: 1
channel: product_supervisor_ack
project: job-hunter
last_seen_directive_id: JH-SUP-0009
last_accepted_directive_id: JH-SUP-0009
last_applied_directive_id: JH-SUP-0009
status: DISCUSSION_COMPLETE
blocker_class: null
updated_at: "2026-09-03T00:55:00Z"
updated_by: claude_active_orchestrator
head_sha: TBD
evidence:
  - docs/design/CLAUDE_NEXT_STEP_AFTER_GOOGLE_BLOCK.md
  - docs/design/CODEX_NEXT_STEP_AFTER_GOOGLE_BLOCK.md
  - docs/design/GEMINI_NEXT_STEP_AFTER_GOOGLE_BLOCK.md
  - docs/design/NEXT_STEP_AFTER_GOOGLE_BLOCK_DECISION.md
notes: >
  JH-SUP-0009 discussion-only directive complete. Claude and Codex answers
  independent (Codex given only the bare problem statement, no prior
  answer shown). Both converge without prompting: revive the frozen,
  already-proven-live JH-SUP-0003 portal-native adapter (profession.hu,
  zero cost, zero blocking, already found 15+ relevant IT-leadership
  postings) as the primary next experiment; Brave Search API (or Google
  PSE, pending current-terms verification) as fallback; explicitly reject
  further Google-browser retries and residential-proxy-as-evasion.

  Gemini recorded GEMINI_NOT_AVAILABLE_NONBLOCKING per directive section C
  -- the only channel was mid-task on unrelated work; not interrupted, not
  waited on.

  No implementation was performed or authorized -- this directive is
  design/decision output only, per its own explicit rule. The broad
  development freeze (JH-SUP-0005/0009) remains in force.

  Also: since this ack was last updated, I set up a persistent background
  watcher (Monitor) polling job-hunter's origin/main every 45s, so future
  SUP-directive commits generate an immediate notification instead of
  requiring the Product Owner to prompt me to check -- a real fix for the
  repeated "why didn't you notice this" gap earlier in this session,
  not just a promise to check more often.
